Para poder ejecutar el código, primero se deben instalar las siguientes librerías:

(Copia y pega estos comando en el cmd o símbolo del sistema)

pip install tk

pip install sympy

pip install numpy

pip install matplotlib
